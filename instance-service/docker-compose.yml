version: '3.4'

services:
  neo4j:
    build:
      context: .
      dockerfile: Dockerfile-neo4j
    environment:
      - NEO4J_AUTH=neo4j/test
      - NEO4J_ACCEPT_LICENSE_AGREEMENT=yes
    ports:
      - "7474:7474"
      - "7687:7687"
