version: '3.4'

services:
  mongo:
    image: mongo
    ports:
      - 27017:27017

  permission-api:
    build: .
    environment:
      PORT: 8000
      MONGO_URI: mongodb://mongo
      MONGO_PERMISSIONS_COLLECTION_NAME: permissions
    ports:
      - 8000:8000
