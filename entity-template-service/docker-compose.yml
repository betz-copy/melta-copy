version: '3.4'

services:
  mongo:
    image: mongo
    ports:
      - 27017:27017

  entity-template-manager:
    build: .
    environment:
      PORT: 8000
      MONGO_URI: mongodb://mongo:27017/entityTemplateManager
      MONGO_ENTITY_TEMPLATE_COLLECTION_NAME: entityTemplate
      MONGO_CATEGORY_COLLECTION_NAME : category
