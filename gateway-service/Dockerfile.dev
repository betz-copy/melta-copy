# syntax=docker/dockerfile:1.4
FROM node:22-alpine

WORKDIR /usr/src/app

# Enable pnpm
RUN corepack enable && corepack prepare pnpm@9.15.0 --activate

COPY package*.json ./
COPY pnpm-lock.yaml ./
COPY tsconfig.json ./
COPY turbo.json ./
COPY pnpm-workspace.yaml ./

# Copy packages (contains package.json files needed for pnpm workspace resolution)
COPY packages ./packages

COPY gateway-service/package*.json ./gateway-service/
COPY gateway-service/.npmrc ./gateway-service/.npmrc
COPY .npmrc ./

RUN --mount=type=secret,id=npm_token \
    NPM_TOKEN=$(cat /run/secrets/npm_token) \
    pnpm install --frozen-lockfile --silent

COPY gateway-service ./gateway-service

# Build using Turborepo (only builds gateway-service and its dependencies)
RUN --mount=type=cache,target=/usr/src/app/.turbo \
    pnpm turbo build --filter=gateway-service

RUN rm -f .npmrc

EXPOSE 8000

CMD ["pnpm", "--filter", "gateway-service", "dev:nodemon"]
