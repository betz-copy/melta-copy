import { createTheme } from '@mui/material';

export const globalTheme = createTheme({
    direction: 'rtl',
    palette: {
        mode: 'light',
    },
    typography: {
        fontFamily: 'Rubik',
    },
});
