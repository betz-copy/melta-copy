input {
   beats{
      port => 5044
   }
}

filter {
   json {
      source => "message"
   }
}

output {
   elasticsearch {
   hosts => "http://elastic:9200"
   index => "%{[extra][serviceName]}-%{[level]}-test-%{+YYYY.MM.DD}"
   user => "elastic"
   password => "password"
 }
}