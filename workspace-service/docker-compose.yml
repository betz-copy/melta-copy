version: '3.4'

services:
  mongo:
    image: mongo
    ports:
      - 27017:27017

  workspace-service:
    build: .
    environment:
      PORT: 8000
      MONGO_URL: mongodb://mongo/workspace-service
      MONGO_WORKSPACES_COLLECTION_NAME: workspaces
    ports:
      - 8001:8000
